services:
  netboxclone-postgres:
    image: postgres:15-alpine
    container_name: netbox_postgres
    hostname: netboxclond-postgres
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "pg_isready", "-q", "-d", "netbox", "-U", "netbox"]
      timeout: 45s
      interval: 10s
      retries: 10   
    volumes:
      - netboxclone_postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
    networks:
      - netboxclone_internal
     
  netboxclone-redis:
    image: redis:7-alpine

    command: redis-server
    container_name: netbox_redis
    hostname: netboxclone-redis
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping || exit 1"]
    restart: always
    volumes:
      - netboxclone_redis:/data
    networks:
      - netboxclone_internal
      
  netboxclone-server:
    image: lscr.io/linuxserver/netbox:latest
    container_name: netbox_server
    hostname: netboxclone-server
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=${TZ}
      - SUPERUSER_EMAIL=${SUPERUSER_EMAIL}
      - SUPERUSER_PASSWORD=${SUPERUSER_PASSWORD}
      - ALLOWED_HOST=${ALLOWED_HOST}
      - DB_NAME=${POSTGRES_DB}
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_HOST=netbox-postgres
      - DB_PORT=5432
      - REDIS_HOST=netbox-redis
      - REDIS_PORT=6379
      - REDIS_DB_TASK=0
      - REDIS_DB_CACHE=1

    volumes:
      - netboxclone_config:/config
    ports:
      - 8001:8000
    depends_on:
      - netboxclone-postgres
      - netboxclone-redis
    restart: unless-stopped
    networks:
      - netboxclone_internal


networks:
  netboxclone_internal:

volumes:
  netboxclone_config:
    driver_opts:
      type: "nfs"
      o: ${NETBOX_NFS_PATH}
      device: ${NETBOX_NFS_DEVICE_CONFIG}
  netboxclone_redis:
    driver_opts:
      type: "nfs"
      o: ${NETBOX_NFS_PATH}
      device: ${NETBOX_NFS_DEVICE_REDISDATA}
  netboxclone_postgres:
    driver_opts:
      type: "nfs"
      o: ${NETBOX_NFS_PATH}
      device: ${NETBOX_NFS_DEVICE_PGDATA}